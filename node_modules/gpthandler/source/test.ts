import { Llm, llmModelsEnum } from "./index";

export async function test() {
    const llm = new Llm();
    llm.setOptions({ retryCount: 3, llmModel: llmModelsEnum.G4f.gpt3_5_t });

    console.log(await llm.requestChat([{ role: 'user', content: 'hi' }]));
    console.log(await llm.translate("ololo", "en", "ru"));
}

// test();